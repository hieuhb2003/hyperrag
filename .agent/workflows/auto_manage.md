---
description: Tự động hoá quản lý project hyperrag
---
// turbo-all

Workflow này cho phép Antigravity tự động thực hiện các thao tác quản lý trong folder hyperrag.

1. Đồng bộ môi trường: `pip install -r requirements.txt`
2. Kiểm tra cấu trúc project: `ls -R`
3. Chạy thử nghiệm code: `python run.py`
4. Sửa lỗi logic hoặc lỗi runtime phát sinh trong folder này.

Lưu ý: Mọi lệnh xóa hoặc sửa đổi ngoài thư mục `/Users/hieunguyenmanh/Desktop/hyperrag` đều bị cấm tự động hóa.
